# packages/shared-ui — shared Angular UI library

**Status: reserved (D-002).** In M1 each web app owns its components. This library is extracted
later once shared patterns stabilise, to avoid two agents contending on one package during M1.
